# Package custom defaults

Place reusable custom Story-Film workflow JSON files here as:

`comfyui_workflows/custom/<task>/<model>/<workflow>.json`

These choices are listed ahead of built-in workflows. Do not put research-only source graphs here.
